// Will be useful one day to check for Auth and abort nav if needed

/*export default defineNuxtRouteMiddleware((to, from) =>
  if (to.params.id === '1') {
    return abortNavigation()
  }
  return navigateTo('/')
})*/